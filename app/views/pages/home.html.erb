<div class= "display-group">
    <div class="container"> 
        <p style="color: green"><%= notice %></p>
        <br>
        <br>
    </div>
    <div class="grid-container container">
        <div class="grid-item">
            <h3> Bienvenido a Page Name: </h3>
        </div>
        <div class="grid-item"> 
                <%= link_to "Realizar una solicitud", new_request_path, class: "btn btn-primary" %>
            <br>
            <br>
                <%= link_to "Ver estado de una solicitud", ask_state_path, class: "btn btn-primary" %>
        </div>
    </div>
    <% if current_user %>
        <p>Tipo de usuario: <%= current_user.role %></p>
    <% else %>
        <p>No hay usuario logueado</p>
    <% end %>
</div>